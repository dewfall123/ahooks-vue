import { useInViewport } from '../index';

// vue2-import-slot

describe('useHover', () => {
  // vue2-beforeAll-slot
  it('should be defined', () => {
    expect(useInViewport).toBeDefined();
    // TODO
  });
});
